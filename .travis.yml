language: node_js
node_js:
  - "5.1.0"
matrix:
  allow_failures:
    - node_js: "5.1.0"
  fast_finish: true
script: "cd app && npm install && npm run-script test-travis"
after_script: "cd app && npm install coveralls@2.10.0 && cat ./coverage/lcov.info | coveralls"
